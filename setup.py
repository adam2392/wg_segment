from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A set of research and models for white/gray matter segmentation using only LFP signals.',
    author='Adam, Chester, Patrick',
    license='MIT',
)
